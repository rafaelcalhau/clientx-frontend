export const SERVICE_PAYMENT_CYCLE_OPTIONS_KEYS = Object.freeze({
  hourly: 'hourly',
  daily: 'daily',
  monthly: 'monthly',
  'one-time': 'one-time',
})

export const SERVICE_PAYMENT_CYCLE_OPTIONS = Object.freeze(
  Object.keys(SERVICE_PAYMENT_CYCLE_OPTIONS_KEYS)
)
